# Public frontend configuration only. Never put secrets in VITE_* values.
# mock = local typed repositories for design/development
# django = real Django API adapters
VITE_DATA_ADAPTER=mock

# Required only when VITE_DATA_ADAPTER=django
# Example: https://api.example.com/
VITE_API_BASE_URL=
