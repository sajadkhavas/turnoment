# TURNOMENT — PROJECT CONTINUITY CHECKPOINT

> **MANDATORY FIRST READ FOR EVERY CHAT / AGENT / SESSION**
>
> Operational source of truth for continuing Turnoment without duplicate work.

Last update: `2026-09-13`

## 1. Mandatory continuation law

Every chat/agent MUST read `PROJECT_CONTINUITY.md`, `FRONTEND_PAGE_DELIVERY_PROTOCOL.md`, `SEO_FINAL_COPY_PROTOCOL.md` for public/indexable work, and `docs/ROUTE_COMPLIANCE_REGISTRY.md`; verify exact live `main`; use a dedicated branch/Issue/PR; preserve frozen routes; keep production fail-closed; and never claim `DONE / MERGED / FROZEN` from chat memory alone.

Terminal closeout SHA/CI belongs in the tracking Issue after merge. Do not create recursive documentation commits to self-record their own SHA.

## 2. Current frontend baseline

Repository: `sajadkhavas/turnoment`.

F24-R1 `/rules` policy reconciliation is terminally closed:
- Issue #110 — `CLOSED / COMPLETED`;
- final F24-R1 main `faf2b18287484d2ed78094589fe2db80f2841f55`;
- final status `DONE / MERGED / FROZEN — FINAL_CURRENT`;
- terminal evidence is recorded in Issue #110.

F25 implementation removed the inherited non-Turnoment ecommerce/service frontend surface.

Exact accepted F25 implementation main / closeout base:

`cb02d65ad9fc5c1d4a27ca775bcb5840f33026c1`

F25:
- Tracking Issue #113 — OPEN until terminal closeout evidence is recorded;
- implementation branch `phase/f25-remove-legacy-routes`;
- implementation head `f9200802c1d67899d018bf23de417fcce017c31c`;
- implementation PR #114 — MERGED with expected-head lock;
- implementation merge/main `cb02d65ad9fc5c1d4a27ca775bcb5840f33026c1`;
- PR-context Full Frontend Quality `34760912403` — PASS — artifact `10319201127` — digest `sha256:03146af8f55b263b353a9170d43be1cd06e566d816a6c02291af4bd16f767e6d`;
- exact-main Full Frontend Quality `34761207642` — PASS — artifact `10319136486` — digest `sha256:944a5b64f199a653644c0510fb7966ea6da634f784536daad18697cc184c2e3d`;
- exact live `main` reverified at the implementation merge after exact-main QA;
- terminal F25 status still requires this docs-only closeout PR, terminal frozen-main QA/evidence, live-main reverify, and Issue #113 closure.

## 3. Accepted Turnoment route inventory

Public/current routes:
- `/` — `FINAL_CURRENT`;
- `/tournaments` — `FINAL_CURRENT`;
- `/games` — `FINAL_CURRENT`;
- `/games/$slug` — `FINAL_CURRENT`;
- `/centers` — `FINAL_CURRENT`;
- `/centers/$id` — `FINAL_CURRENT`;
- `/ranking` — `FINAL_CURRENT`;
- `/players/$username` — `FINAL_CURRENT`;
- `/host` — `FINAL_CURRENT`;
- `/rules` — `FINAL_CURRENT`.

Public route still awaiting current-law recertification:
- `/tournaments/$id` — `FINAL_PRE_SEO`.

Accepted private/noindex routes:
- `/tournaments/$id/register`;
- `/dashboard`;
- `/dashboard/tournaments`;
- `/dashboard/matches`;
- `/matches/$id/result`;
- `/matches/$id/dispute`;
- `/login`;
- `/register`;
- `/dashboard/profile`;
- `/dashboard/notifications`;
- `/dashboard/settings`;
- `/dashboard/rivalries`;
- `/dashboard/achievements`;
- `/dashboard/teams`;
- `/dashboard/challenges`.

These accepted routes remain protected from unrelated source mutation.

## 4. F25 product decision — legacy surface removed

The product owner explicitly decided that every existing route classified as `LEGACY_REVIEW` and unrelated to the Turnoment tournament product must be deleted rather than redesigned or recertified.

F25 removed these 17 routes:
- `/about`;
- `/blog`;
- `/blog/$slug`;
- `/cart`;
- `/category/$slug`;
- `/checkout`;
- `/contact`;
- `/faq`;
- `/payment/result`;
- `/products`;
- `/products/$slug`;
- `/services`;
- `/services/request`;
- `/dashboard/addresses`;
- `/dashboard/orders`;
- `/dashboard/services`;
- `/dashboard/wishlist`.

These routes are intentionally absent. They are not backlog items and must not be revived merely because the old Iran Mehr Afzar README mentions ecommerce/service features.

F25 also removed the inherited legacy site shell/product/card/cart/mock-data production dependencies and removed the global cart provider. Root fallback/default metadata now identify Turnoment rather than Iran Mehr Afzar.

`src/routeTree.gen.ts` is generated by the TanStack/Vite build path. F25 does not hand-author it; both PR-context and exact-main production build + route generation passed before typecheck and browser QA.

## 5. Active workstream — F25 closeout

Tracking Issue: `#113`.

Implementation START:

`faf2b18287484d2ed78094589fe2db80f2841f55`

Implementation head:

`f9200802c1d67899d018bf23de417fcce017c31c`

Implementation PR:

`#114` — MERGED.

Accepted implementation main / closeout base:

`cb02d65ad9fc5c1d4a27ca775bcb5840f33026c1`

Closeout branch:

`closeout/f25-remove-legacy-routes`

Current status:

`IMPLEMENTATION MERGED / POST-MAIN ACCEPTED / CLOSEOUT IN PROGRESS`

F25 must not be reported terminally `DONE / MERGED / FROZEN` until the closeout PR is merged with expected-head lock, terminal frozen-main QA passes, terminal artifacts/digests are recorded in Issue #113, exact live `main` is reverified, and Issue #113 is closed completed.

## 6. F25 closeout law

This closeout is documentation-only and must be exactly one commit changing exactly these four Markdown files:
1. `PROJECT_CONTINUITY.md`;
2. `docs/ROUTE_COMPLIANCE_REGISTRY.md`;
3. `docs/workstreams/F25_LEGACY_ROUTE_REMOVAL.md`;
4. `docs/workstreams/F25_CLOSEOUT.md`.

No source, package, lockfile, workflow, dependency, runtime configuration or backend mutation is authorized in closeout.

Future closeout-head/merge/frozen-main facts must be recorded in Issue #113 after they exist, not recursively self-recorded in a follow-up commit.

## 7. NEXT

Immediate task: complete the F25 documentation-only closeout and terminal evidence chain.

After terminal F25, frontend NEXT is `/tournaments/$id` current-law public recertification.

Backend NEXT independently remains `P02 — Games / Catalog Foundation`.
